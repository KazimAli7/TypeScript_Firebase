/* eslint-disable no-unused-vars */
import React from 'react';
import { SET_EMAIL, SET_PASSWORD } from '../selectors';

const initialState = {
  users: [],
};

function ProjectReducer(state = initialState, action: any) {
  return state;
}

export default ProjectReducer;
